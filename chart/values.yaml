---
# LLM configuration
#
# Option A: Bring your own model (MaaS - Model as a Service)
# model:
#   name: my-model
#   endpoint: https://my-maas-instance:443
#   api_key: my-api-key
#
# Option B: Deploy a model with this chart
model: {}

# Application configuration
app:
  replicas: 1
  image: quay.io/rh-ai-quickstart/enterprise-rag-intel-continuum:latest

# RAG pipeline configuration
rag:
  embedding_model: nomic-embed-text
  generation_model: qwen2.5:1.5b
  chunk_size: 512
  chunk_overlap: 50
  demo_mode: true

# Ollama configuration (for laptop/dev mode)
ollama:
  enabled: true
  image: ollama/ollama:latest
  models:
    - nomic-embed-text
    - qwen2.5:1.5b
